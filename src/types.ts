export enum RoutePath {
  MainPage = "/",
  ContactsPage = "/contacts",
  RequisitesPage = "/requisites",
  EmailPage = "/emailpage",
}

export interface ProductBanner {
  title: string;
  marks: string;
  products: string;
  product_bg: string;
  icons: string[];
}
